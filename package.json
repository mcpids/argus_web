{
  "name": "paart-website",
  "version": "1.0.0",
  "private": true,
  "description": "Paart marketing site (paart.ai)",
  "scripts": {
    "build": "tailwindcss -i src/input.css -o dist/tailwind.css --minify",
    "watch": "tailwindcss -i src/input.css -o dist/tailwind.css --watch"
  },
  "devDependencies": {
    "tailwindcss": "^3.4.17"
  }
}
